# Discord Bot Tokens (comma-separated, must match active_bots order in config.json)
DISCORD_BOT_TOKENS=token1,token2,token3

# Default Guild/Server ID (optional, can be set per-bot in config.json)
DISCORD_GUILD_ID=

# Connectome gRPC Server
CONNECTOME_GRPC_HOST=connectome:50051

# LLM Provider - Anthropic API (for non-Bedrock models)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# LLM Provider - AWS Bedrock (for bedrock- prefixed models)
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1
